Для 64 битной jvm, с вкл. компрессией -XX:+UseCompressedOops.

2. Объект User(true, "Tom", 27) заголовок займёт 12байт, 
   private boolean married;  - займёт 1байт,
   private int age; - займёт 4байта,
   private String name; - объект займёт 50байт(
     - ссылка на стринг - займёт 4байтa,
     - заголовок String - займёт 12байт,
     - поле hash int - займёт 4байта,
     - поле coder byte - займёт 1байт,
     - поле hashIsZero boolean - займёт 1байт,
     - ссылка на массив char займёт 4байта,
     - заголовок массива займёт 16байт,
     - три символа в массиве 6байт с округлением до 8);
    
  Общий объем выделенной памяти составит 72байта(67 до округления);

3.Обьект без полей(только указатель) будет занимать 12байт округленных
  до 16байт. В указатель входит:
   - hash
   - возраст (количество пережитых сборок мусора)
   - состояние блокировки
   - количество ссылок на обьект
   - тип обьекта

5.Запуск приложения с уменьшенным размером хипа:
  java -Xmx10m -Xms10m -XX:+UseSerialGC -jar C:\projects\job4j_design\target\GC.jar
  После создания 542-ого объекта User сработал минорный тип GC. 
  Объекты без ссылок были удалены, что подтверждает вызов у этих 
  объектов метода finalize. Объекты которые сохранили ссылки были
  перенесены из эдема в сурвайвл S0.